import HomePage from '@/components/home/HomePage';

/**
 * The main page component that renders the ProblemDriven HomePage.
 *
 * @returns {JSX.Element} The rendered HomePage component.
 */
const Page = () => {
    return <HomePage />;
};

export default Page;
